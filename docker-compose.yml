services:
  qa:
    build:
      context: .
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/tmp/composer-cache
    command: composer qa

  test:
    build:
      context: .
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/tmp/composer-cache
    command: composer test

  shell:
    build:
      context: .
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/tmp/composer-cache
    command: sh
    profiles:
      - dev

volumes:
  composer-cache:
